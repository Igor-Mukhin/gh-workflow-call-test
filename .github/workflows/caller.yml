name: Caller

on:
  workflow_dispatch:
    inputs:
      input1:
        description: "A number input"
        required: false
        type: number
        default: 42
jobs:
  main:
    uses: ./.github/workflows/callee.yml
    with:
      input1: ${{ inputs.input1 }}
