name: Caller

on:
  workflow_dispatch:
    inputs:
      input1:
        description: "A number input"
        required: true
        type: number
        default: 42

jobs:
  caller:
    uses: ./.github/workflows/callee.yml
    with:
      input1: 11
