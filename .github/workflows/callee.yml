name: Callee

on:
  workflow_call:
    inputs:
      input2:
        description: "A number input"
        required: true
        type: string
        default: "42"

jobs:
  callee:
    runs-on: ubuntu-latest

    steps:
      - name: Use the input
        run: |
          echo "The input value is ${{ inputs.input2 }}"
